---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: oauth2s.security.nexus.m.tinyorbit.vn
spec:
  group: security.nexus.m.tinyorbit.vn
  names:
    categories:
    - crossplane
    - managed
    - sonatyperepo
    kind: Oauth2
    listKind: Oauth2List
    plural: oauth2s
    singular: oauth2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Oauth2 is the Schema for the Oauth2s API. Configure Sonatype
          Nexus Repository OAuth2 / OpenID Connect (OIDC) authentication. Requires
          Nexus Repository Pro 3.94.0 or later, with nexus.security.oauth2.enabled=true
          and nexus.jwt.enabled=true set in nexus.properties. The underlying API does
          not exist on earlier versions - applying this resource against an older
          server fails with a clear error rather than a panic.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Oauth2Spec defines the desired state of Oauth2
            properties:
              forProvider:
                properties:
                  authorizationCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the authorization request
                      Additional custom parameters sent with the authorization request
                    type: object
                    x-kubernetes-map-type: granular
                  clientId:
                    description: |-
                      (String) Client ID issued by the OpenID Provider for this Nexus Repository instance
                      Client ID issued by the OpenID Provider for this Nexus Repository instance
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      of-band changes to it.
                      Client Secret issued by the OpenID Provider.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  emailClaim:
                    description: |-
                      (String) Claim containing the user's email address
                      Claim containing the user's email address
                    type: string
                  exactMatchClaims:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Claims that must exactly match the given value for authentication to succeed
                      Claims that must exactly match the given value for authentication to succeed
                    type: object
                    x-kubernetes-map-type: granular
                  firstNameClaim:
                    description: |-
                      (String) Claim containing the user's first name
                      Claim containing the user's first name
                    type: string
                  groupsClaim:
                    description: |-
                      (String) Claim containing the user's group memberships, required for role mapping
                      Claim containing the user's group memberships, required for role mapping
                    type: string
                  idpAuthorizationUrl:
                    description: |-
                      (String) Authorization endpoint of the OpenID Provider
                      Authorization endpoint of the OpenID Provider
                    type: string
                  idpJwks:
                    description: |-
                      (String) Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                      Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                    type: string
                  idpJwksUrl:
                    description: |-
                      (String) JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                      JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                    type: string
                  idpJwsAlgorithm:
                    description: |-
                      e.g. RS256
                      JWT signature algorithm used to validate tokens issued by the OpenID Provider - e.g. RS256
                    type: string
                  idpLogoutUrl:
                    description: |-
                      (String) Logout endpoint of the OpenID Provider
                      Logout endpoint of the OpenID Provider
                    type: string
                  idpTokenUrl:
                    description: |-
                      (String) Token endpoint of the OpenID Provider
                      Token endpoint of the OpenID Provider
                    type: string
                  lastNameClaim:
                    description: |-
                      (String) Claim containing the user's last name
                      Claim containing the user's last name
                    type: string
                  tokenRequestCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the token request
                      Additional custom parameters sent with the token request
                    type: object
                    x-kubernetes-map-type: granular
                  useTrustStore:
                    description: |-
                      (Boolean) Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                      Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                    type: boolean
                  usernameClaim:
                    description: |-
                      (String) Claim used as the unique identifier for the user
                      Claim used as the unique identifier for the user
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizationCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the authorization request
                      Additional custom parameters sent with the authorization request
                    type: object
                    x-kubernetes-map-type: granular
                  clientId:
                    description: |-
                      (String) Client ID issued by the OpenID Provider for this Nexus Repository instance
                      Client ID issued by the OpenID Provider for this Nexus Repository instance
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      of-band changes to it.
                      Client Secret issued by the OpenID Provider.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  emailClaim:
                    description: |-
                      (String) Claim containing the user's email address
                      Claim containing the user's email address
                    type: string
                  exactMatchClaims:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Claims that must exactly match the given value for authentication to succeed
                      Claims that must exactly match the given value for authentication to succeed
                    type: object
                    x-kubernetes-map-type: granular
                  firstNameClaim:
                    description: |-
                      (String) Claim containing the user's first name
                      Claim containing the user's first name
                    type: string
                  groupsClaim:
                    description: |-
                      (String) Claim containing the user's group memberships, required for role mapping
                      Claim containing the user's group memberships, required for role mapping
                    type: string
                  idpAuthorizationUrl:
                    description: |-
                      (String) Authorization endpoint of the OpenID Provider
                      Authorization endpoint of the OpenID Provider
                    type: string
                  idpJwks:
                    description: |-
                      (String) Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                      Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                    type: string
                  idpJwksUrl:
                    description: |-
                      (String) JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                      JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                    type: string
                  idpJwsAlgorithm:
                    description: |-
                      e.g. RS256
                      JWT signature algorithm used to validate tokens issued by the OpenID Provider - e.g. RS256
                    type: string
                  idpLogoutUrl:
                    description: |-
                      (String) Logout endpoint of the OpenID Provider
                      Logout endpoint of the OpenID Provider
                    type: string
                  idpTokenUrl:
                    description: |-
                      (String) Token endpoint of the OpenID Provider
                      Token endpoint of the OpenID Provider
                    type: string
                  lastNameClaim:
                    description: |-
                      (String) Claim containing the user's last name
                      Claim containing the user's last name
                    type: string
                  tokenRequestCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the token request
                      Additional custom parameters sent with the token request
                    type: object
                    x-kubernetes-map-type: granular
                  useTrustStore:
                    description: |-
                      (Boolean) Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                      Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                    type: boolean
                  usernameClaim:
                    description: |-
                      (String) Claim used as the unique identifier for the user
                      Claim used as the unique identifier for the user
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.idpJwsAlgorithm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.idpJwsAlgorithm)
                || (has(self.initProvider) && has(self.initProvider.idpJwsAlgorithm))'
            - message: spec.forProvider.usernameClaim is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.usernameClaim)
                || (has(self.initProvider) && has(self.initProvider.usernameClaim))'
          status:
            description: Oauth2Status defines the observed state of Oauth2.
            properties:
              atProvider:
                properties:
                  authorizationCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the authorization request
                      Additional custom parameters sent with the authorization request
                    type: object
                    x-kubernetes-map-type: granular
                  clientId:
                    description: |-
                      (String) Client ID issued by the OpenID Provider for this Nexus Repository instance
                      Client ID issued by the OpenID Provider for this Nexus Repository instance
                    type: string
                  emailClaim:
                    description: |-
                      (String) Claim containing the user's email address
                      Claim containing the user's email address
                    type: string
                  exactMatchClaims:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Claims that must exactly match the given value for authentication to succeed
                      Claims that must exactly match the given value for authentication to succeed
                    type: object
                    x-kubernetes-map-type: granular
                  firstNameClaim:
                    description: |-
                      (String) Claim containing the user's first name
                      Claim containing the user's first name
                    type: string
                  groupsClaim:
                    description: |-
                      (String) Claim containing the user's group memberships, required for role mapping
                      Claim containing the user's group memberships, required for role mapping
                    type: string
                  id:
                    type: string
                  idpAuthorizationUrl:
                    description: |-
                      (String) Authorization endpoint of the OpenID Provider
                      Authorization endpoint of the OpenID Provider
                    type: string
                  idpJwks:
                    description: |-
                      (String) Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                      Literal JSON Web Key Set (JWKS) document, as an alternative to idp_jwks_url
                    type: string
                  idpJwksUrl:
                    description: |-
                      (String) JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                      JSON Web Key Set (JWKS) endpoint of the OpenID Provider
                    type: string
                  idpJwsAlgorithm:
                    description: |-
                      e.g. RS256
                      JWT signature algorithm used to validate tokens issued by the OpenID Provider - e.g. RS256
                    type: string
                  idpLogoutUrl:
                    description: |-
                      (String) Logout endpoint of the OpenID Provider
                      Logout endpoint of the OpenID Provider
                    type: string
                  idpTokenUrl:
                    description: |-
                      (String) Token endpoint of the OpenID Provider
                      Token endpoint of the OpenID Provider
                    type: string
                  lastNameClaim:
                    description: |-
                      (String) Claim containing the user's last name
                      Claim containing the user's last name
                    type: string
                  lastUpdated:
                    description: |-
                      (String) String representation of the date/time the resource was last changed
                      String representation of the date/time the resource was last changed
                    type: string
                  tokenRequestCustomParams:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Additional custom parameters sent with the token request
                      Additional custom parameters sent with the token request
                    type: object
                    x-kubernetes-map-type: granular
                  useTrustStore:
                    description: |-
                      (Boolean) Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                      Whether to validate the OpenID Provider's certificate using Nexus Repository's own truststore
                    type: boolean
                  usernameClaim:
                    description: |-
                      (String) Claim used as the unique identifier for the user
                      Claim used as the unique identifier for the user
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
