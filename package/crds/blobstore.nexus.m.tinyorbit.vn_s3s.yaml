---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: s3s.blobstore.nexus.m.tinyorbit.vn
spec:
  group: blobstore.nexus.m.tinyorbit.vn
  names:
    categories:
    - crossplane
    - managed
    - sonatyperepo
    kind: S3
    listKind: S3List
    plural: s3s
    singular: s3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: S3 is the Schema for the S3s API. Use this resource to create
          a S3 Blob Store
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: S3Spec defines the desired state of S3
            properties:
              forProvider:
                properties:
                  bucketConfiguration:
                    description: (Attributes) Bucket Configuration for this Blob Store
                      (see below for nested schema)
                    properties:
                      advancedBucketConnection:
                        description: (Attributes) Advanced Connection Configuration
                          for this S3 Blob Store (see below for nested schema)
                        properties:
                          endpoint:
                            description: |-
                              (String) A custom endpoint URL for third party object stores using the S3 API
                              A custom endpoint URL for third party object stores using the S3 API
                            type: string
                          forcePathStyle:
                            description: |-
                              style access being used for all requests
                              Setting this flag will result in path-style access being used for all requests
                            type: boolean
                          maxConnectionPoolSize:
                            description: |-
                              (Number) Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                              Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                            type: number
                          signerType:
                            description: |-
                              (String) An API signature version which may be required for third party object stores using the S3 API
                              An API signature version which may be required for third party object stores using the S3 API
                            type: string
                        type: object
                      bucket:
                        description: (Attributes) Main Bucket Configuration for this
                          Blob Store (see below for nested schema)
                        properties:
                          name:
                            description: |-
                              (String) Name of the Blob Store
                              The name of the S3 bucket
                            type: string
                          prefix:
                            description: |-
                              (String) The S3 blob store (i.e S3 object) key prefix
                              The S3 blob store (i.e S3 object) key prefix
                            type: string
                          region:
                            description: |-
                              (String) The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                              The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                            type: string
                        type: object
                      bucketSecurity:
                        description: (Attributes) Bucket Security Configuration for
                          this Blob Store (see below for nested schema)
                        properties:
                          accessKeyIdSecretRef:
                            description: |-
                              (String, Sensitive) An IAM access key ID for granting access to the S3 bucket
                              An IAM access key ID for granting access to the S3 bucket
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          role:
                            description: |-
                              (String) An IAM role to assume in order to access the S3 bucket
                              An IAM role to assume in order to access the S3 bucket
                            type: string
                          secretAccessKeySecretRef:
                            description: |-
                              (String, Sensitive) The secret access key associated with the specified IAM access key ID
                              The secret access key associated with the specified IAM access key ID
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          sessionTokenSecretRef:
                            description: |-
                              (String, Sensitive) An AWS STS session token associated with temporary security credentials which grant access to the S3 bucket
                              An AWS STS session token associated with temporary security credentials which grant access to the S3 bucket
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                      encryption:
                        description: (Attributes) Bucket Encryption Configuration
                          for this Blob Store (see below for nested schema)
                        properties:
                          encryptionKeySecretRef:
                            description: |-
                              (String, Sensitive) The encryption key
                              The encryption key
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          encryptionType:
                            description: |-
                              (String) The type of S3 server side encryption to use
                              The type of S3 server side encryption to use
                            type: string
                        type: object
                      preSignedUrlEnabled:
                        description: |-
                          signed URL is enabled or not. Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later
                          Whether pre-signed URL is enabled or not. **Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later**
                        type: boolean
                    type: object
                  softQuota:
                    description: (Attributes) Soft Quota for this Blob Store (see
                      below for nested schema)
                    properties:
                      limit:
                        description: |-
                          (Number) Quota limit
                          Quota limit
                        type: number
                      type:
                        description: |-
                          will always be 's3'
                          Soft Quota type
                        type: string
                    type: object
                  type:
                    description: |-
                      will always be 's3'
                      Type of this Blob Store - will always be 's3'
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  bucketConfiguration:
                    description: (Attributes) Bucket Configuration for this Blob Store
                      (see below for nested schema)
                    properties:
                      advancedBucketConnection:
                        description: (Attributes) Advanced Connection Configuration
                          for this S3 Blob Store (see below for nested schema)
                        properties:
                          endpoint:
                            description: |-
                              (String) A custom endpoint URL for third party object stores using the S3 API
                              A custom endpoint URL for third party object stores using the S3 API
                            type: string
                          forcePathStyle:
                            description: |-
                              style access being used for all requests
                              Setting this flag will result in path-style access being used for all requests
                            type: boolean
                          maxConnectionPoolSize:
                            description: |-
                              (Number) Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                              Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                            type: number
                          signerType:
                            description: |-
                              (String) An API signature version which may be required for third party object stores using the S3 API
                              An API signature version which may be required for third party object stores using the S3 API
                            type: string
                        type: object
                      bucket:
                        description: (Attributes) Main Bucket Configuration for this
                          Blob Store (see below for nested schema)
                        properties:
                          name:
                            description: |-
                              (String) Name of the Blob Store
                              The name of the S3 bucket
                            type: string
                          prefix:
                            description: |-
                              (String) The S3 blob store (i.e S3 object) key prefix
                              The S3 blob store (i.e S3 object) key prefix
                            type: string
                          region:
                            description: |-
                              (String) The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                              The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                            type: string
                        type: object
                      bucketSecurity:
                        description: (Attributes) Bucket Security Configuration for
                          this Blob Store (see below for nested schema)
                        properties:
                          accessKeyIdSecretRef:
                            description: |-
                              (String, Sensitive) An IAM access key ID for granting access to the S3 bucket
                              An IAM access key ID for granting access to the S3 bucket
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          role:
                            description: |-
                              (String) An IAM role to assume in order to access the S3 bucket
                              An IAM role to assume in order to access the S3 bucket
                            type: string
                          secretAccessKeySecretRef:
                            description: |-
                              (String, Sensitive) The secret access key associated with the specified IAM access key ID
                              The secret access key associated with the specified IAM access key ID
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          sessionTokenSecretRef:
                            description: |-
                              (String, Sensitive) An AWS STS session token associated with temporary security credentials which grant access to the S3 bucket
                              An AWS STS session token associated with temporary security credentials which grant access to the S3 bucket
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                      encryption:
                        description: (Attributes) Bucket Encryption Configuration
                          for this Blob Store (see below for nested schema)
                        properties:
                          encryptionKeySecretRef:
                            description: |-
                              (String, Sensitive) The encryption key
                              The encryption key
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          encryptionType:
                            description: |-
                              (String) The type of S3 server side encryption to use
                              The type of S3 server side encryption to use
                            type: string
                        type: object
                      preSignedUrlEnabled:
                        description: |-
                          signed URL is enabled or not. Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later
                          Whether pre-signed URL is enabled or not. **Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later**
                        type: boolean
                    type: object
                  softQuota:
                    description: (Attributes) Soft Quota for this Blob Store (see
                      below for nested schema)
                    properties:
                      limit:
                        description: |-
                          (Number) Quota limit
                          Quota limit
                        type: number
                      type:
                        description: |-
                          will always be 's3'
                          Soft Quota type
                        type: string
                    type: object
                  type:
                    description: |-
                      will always be 's3'
                      Type of this Blob Store - will always be 's3'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.bucketConfiguration is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.bucketConfiguration)
                || (has(self.initProvider) && has(self.initProvider.bucketConfiguration))'
          status:
            description: S3Status defines the observed state of S3.
            properties:
              atProvider:
                properties:
                  bucketConfiguration:
                    description: (Attributes) Bucket Configuration for this Blob Store
                      (see below for nested schema)
                    properties:
                      advancedBucketConnection:
                        description: (Attributes) Advanced Connection Configuration
                          for this S3 Blob Store (see below for nested schema)
                        properties:
                          endpoint:
                            description: |-
                              (String) A custom endpoint URL for third party object stores using the S3 API
                              A custom endpoint URL for third party object stores using the S3 API
                            type: string
                          forcePathStyle:
                            description: |-
                              style access being used for all requests
                              Setting this flag will result in path-style access being used for all requests
                            type: boolean
                          maxConnectionPoolSize:
                            description: |-
                              (Number) Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                              Setting this value will override the default connection pool size of Nexus of the s3 client for this blobstore
                            type: number
                          signerType:
                            description: |-
                              (String) An API signature version which may be required for third party object stores using the S3 API
                              An API signature version which may be required for third party object stores using the S3 API
                            type: string
                        type: object
                      bucket:
                        description: (Attributes) Main Bucket Configuration for this
                          Blob Store (see below for nested schema)
                        properties:
                          name:
                            description: |-
                              (String) Name of the Blob Store
                              The name of the S3 bucket
                            type: string
                          prefix:
                            description: |-
                              (String) The S3 blob store (i.e S3 object) key prefix
                              The S3 blob store (i.e S3 object) key prefix
                            type: string
                          region:
                            description: |-
                              (String) The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                              The AWS region to create a new S3 bucket in or an existing S3 bucket's region
                            type: string
                        type: object
                      bucketSecurity:
                        description: (Attributes) Bucket Security Configuration for
                          this Blob Store (see below for nested schema)
                        properties:
                          role:
                            description: |-
                              (String) An IAM role to assume in order to access the S3 bucket
                              An IAM role to assume in order to access the S3 bucket
                            type: string
                        type: object
                      encryption:
                        description: (Attributes) Bucket Encryption Configuration
                          for this Blob Store (see below for nested schema)
                        properties:
                          encryptionType:
                            description: |-
                              (String) The type of S3 server side encryption to use
                              The type of S3 server side encryption to use
                            type: string
                        type: object
                      preSignedUrlEnabled:
                        description: |-
                          signed URL is enabled or not. Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later
                          Whether pre-signed URL is enabled or not. **Requires Sonatype Nexus Repository Manager 3.79.0 PRO or later**
                        type: boolean
                    type: object
                  id:
                    type: string
                  lastUpdated:
                    description: |-
                      (String) String representation of the date/time the resource was last changed
                      String representation of the date/time the resource was last changed
                    type: string
                  softQuota:
                    description: (Attributes) Soft Quota for this Blob Store (see
                      below for nested schema)
                    properties:
                      limit:
                        description: |-
                          (Number) Quota limit
                          Quota limit
                        type: number
                      type:
                        description: |-
                          will always be 's3'
                          Soft Quota type
                        type: string
                    type: object
                  type:
                    description: |-
                      will always be 's3'
                      Type of this Blob Store - will always be 's3'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
