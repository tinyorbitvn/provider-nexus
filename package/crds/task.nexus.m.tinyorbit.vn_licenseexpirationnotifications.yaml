---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: licenseexpirationnotifications.task.nexus.m.tinyorbit.vn
spec:
  group: task.nexus.m.tinyorbit.vn
  names:
    categories:
    - crossplane
    - managed
    - sonatyperepo
    kind: LicenseExpirationNotification
    listKind: LicenseExpirationNotificationList
    plural: licenseexpirationnotifications
    singular: licenseexpirationnotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LicenseExpirationNotification is the Schema for the LicenseExpirationNotifications
          API. Manage Task 'License - Check license expiration and send notifications'
          (license.expiration.notification) This task requires the Sonatype Nexus
          Repository >= 3.86.0 - see official documentation https://help.sonatype.com/en/sonatype-nexus-repository-3-86-0-release-notes.html#license-expiry-notification-and-status-check.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LicenseExpirationNotificationSpec defines the desired state
              of LicenseExpirationNotification
            properties:
              forProvider:
                properties:
                  alertEmail:
                    description: |-
                      mail address for task notifications.
                      E-mail address for task notifications.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Indicates if the task is enabled.
                      Indicates if the task is enabled.
                    type: boolean
                  frequency:
                    description: (Attributes) Frequency Schedule for this Task. (see
                      below for nested schema)
                    properties:
                      cronExpression:
                        description: |-
                          (String) Cron expression for the task. Only applies for for "cron" schedule.
                          Cron expression for the task. Only applies for for "cron" schedule.
                        type: string
                      recurringDays:
                        description: |-
                          (List of Number) Array with the number of the days the task must run.
                          Array with the number of the days the task must run.

                          - For "weekly" schedule allowed values, 1 to 7.
                          - For "monthly" schedule allowed values, 1 to 31.
                        items:
                          type: number
                        type: array
                      schedule:
                        description: |-
                          (String) Type of Schedule.
                          Type of Schedule.
                        type: string
                      startDate:
                        description: |-
                          04-26 01:30 UTC). Does not apply for "manual" schedule.
                          Start date of the task represented in unix timestamp. Sonatype Nexus Repository persists this in milliseconds, so values for any modern date are 13 digits (e.g. `1777167000000` for 2026-04-26 01:30 UTC). Does not apply for "manual" schedule.
                        type: number
                      timezoneOffset:
                        description: |-
                          05:00
                          The offset time zone of the client. Example: -05:00
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) The name of the Task.
                      The name of the Task.
                    type: string
                  notificationCondition:
                    description: |-
                      (String) The type of Task.
                      The type of Task.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertEmail:
                    description: |-
                      mail address for task notifications.
                      E-mail address for task notifications.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Indicates if the task is enabled.
                      Indicates if the task is enabled.
                    type: boolean
                  frequency:
                    description: (Attributes) Frequency Schedule for this Task. (see
                      below for nested schema)
                    properties:
                      cronExpression:
                        description: |-
                          (String) Cron expression for the task. Only applies for for "cron" schedule.
                          Cron expression for the task. Only applies for for "cron" schedule.
                        type: string
                      recurringDays:
                        description: |-
                          (List of Number) Array with the number of the days the task must run.
                          Array with the number of the days the task must run.

                          - For "weekly" schedule allowed values, 1 to 7.
                          - For "monthly" schedule allowed values, 1 to 31.
                        items:
                          type: number
                        type: array
                      schedule:
                        description: |-
                          (String) Type of Schedule.
                          Type of Schedule.
                        type: string
                      startDate:
                        description: |-
                          04-26 01:30 UTC). Does not apply for "manual" schedule.
                          Start date of the task represented in unix timestamp. Sonatype Nexus Repository persists this in milliseconds, so values for any modern date are 13 digits (e.g. `1777167000000` for 2026-04-26 01:30 UTC). Does not apply for "manual" schedule.
                        type: number
                      timezoneOffset:
                        description: |-
                          05:00
                          The offset time zone of the client. Example: -05:00
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) The name of the Task.
                      The name of the Task.
                    type: string
                  notificationCondition:
                    description: |-
                      (String) The type of Task.
                      The type of Task.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.frequency is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.frequency)
                || (has(self.initProvider) && has(self.initProvider.frequency))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.notificationCondition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.notificationCondition)
                || (has(self.initProvider) && has(self.initProvider.notificationCondition))'
          status:
            description: LicenseExpirationNotificationStatus defines the observed
              state of LicenseExpirationNotification.
            properties:
              atProvider:
                properties:
                  alertEmail:
                    description: |-
                      mail address for task notifications.
                      E-mail address for task notifications.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Indicates if the task is enabled.
                      Indicates if the task is enabled.
                    type: boolean
                  frequency:
                    description: (Attributes) Frequency Schedule for this Task. (see
                      below for nested schema)
                    properties:
                      cronExpression:
                        description: |-
                          (String) Cron expression for the task. Only applies for for "cron" schedule.
                          Cron expression for the task. Only applies for for "cron" schedule.
                        type: string
                      recurringDays:
                        description: |-
                          (List of Number) Array with the number of the days the task must run.
                          Array with the number of the days the task must run.

                          - For "weekly" schedule allowed values, 1 to 7.
                          - For "monthly" schedule allowed values, 1 to 31.
                        items:
                          type: number
                        type: array
                      schedule:
                        description: |-
                          (String) Type of Schedule.
                          Type of Schedule.
                        type: string
                      startDate:
                        description: |-
                          04-26 01:30 UTC). Does not apply for "manual" schedule.
                          Start date of the task represented in unix timestamp. Sonatype Nexus Repository persists this in milliseconds, so values for any modern date are 13 digits (e.g. `1777167000000` for 2026-04-26 01:30 UTC). Does not apply for "manual" schedule.
                        type: number
                      timezoneOffset:
                        description: |-
                          05:00
                          The offset time zone of the client. Example: -05:00
                        type: string
                    type: object
                  id:
                    description: (String) The internal ID of the Task.
                    type: string
                  lastUpdated:
                    description: |-
                      (String) String representation of the date/time the resource was last changed
                      String representation of the date/time the resource was last changed
                    type: string
                  name:
                    description: |-
                      (String) The name of the Task.
                      The name of the Task.
                    type: string
                  notificationCondition:
                    description: |-
                      (String) The type of Task.
                      The type of Task.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
